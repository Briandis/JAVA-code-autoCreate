v9.8:
	1、针对一表中同时有两个两个指向本表的代码优化
	2、我也忘了更新了啥
v9.5：
	1、新增页面cloneModel.html
		该页面功能主要针对同一张表但不同操作的而编写的页面，可以用于生成镜像代码
		该模块主要针对一表多用场景设计
	2、修复了V9版中错误的xml语句，并修改了updateByObj
	3、新添加updateByKeyAndWher接口，用于更新条件更新该主键内容

V9：
	1、完全图形化
	2、采用了服务器方式启动
	3、修复了V8若干项BUG
		均和查询有关，更新语句BUG修复
V8:
	1、加入了前台、后台接口
		前台接口不变，后台以/admin形式出现
	2、依赖文件小更新
	3、对POJO生成策略进行更改，现会把自动生成的设置为抽象类，原有POJO为空文件，但会继承自动生成类
	4、添加了条件修改，条件set=null，和查询、计数等
	5、修改了保存或更新、唯一性插入等接口，第一个参数为保存的数据，第二个为筛选条件，建议先看mapper后再使用
	6、由于变动内容和生成策略发生改变，无法保证生成准确性，存在高BUG风险
	7、附赠无文档说明的StringUtil

V7.1:
	1、xmlmapper文件采用引用方式，
	2、加入不想生成的文件选项
	3、修复了控制层中，更新方法直接使用主键，而不是通过封装方式问题。
	4、修复V7多对多致命错误，以及关系解析错误
	5、修复page依赖错误最大页数
	6、解决了fastJson线程不安全问题，在jsonUitl中根据线程自动构造对应实例

V7：
	1、mapper加入多条插入接口
	2、加入只查询一个接口
	3、加入对对多内联、外联查询
	4、将原有的条件插入语句进行更新，才有DUAL临时表，已解决条件插入语无主键返回问题
	5、对控制层，修改、删除，加入非空校验
	6、将mapper接口和xml自动生成文件分离，区别于用户手动编写的xml
